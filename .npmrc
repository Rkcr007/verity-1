# Hoisting controls for a pnpm monorepo with native modules (better-sqlite3)
# and Electron. Electron + native bindings must be available at the root.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
