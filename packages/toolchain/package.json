{
  "name": "@verity/toolchain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One-click JDK/Maven/Node toolchain install for Verity (M7).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "node --import tsx --test 'src/**/*.test.ts'",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@verity/core": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
