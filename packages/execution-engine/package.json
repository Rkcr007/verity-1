{
  "name": "@verity/execution-engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local test execution — Maven subprocess, JUnit output parsing (M5).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "node --import tsx --test 'src/**/*.test.ts'",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@verity/adapter-contract": "workspace:*",
    "@verity/core": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
